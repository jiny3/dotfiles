{{- $passphrase := promptStringOnce . "passphrase" "Enter GPG symmetric passphrase" -}}

encryption = "gpg"

[gpg]
    symmetric = true
    args = ["--batch", "--passphrase", {{ $passphrase | quote }}, "--no-symkey-cache", "--quiet"]
